export interface Traveller {
    passengerId: number;
    firstName: string;
    lastName: string;
    email: string;
}

export interface TravellerDto{
    firstName: string;
    lastName: string;
    email: string;
}